- flash.each do |key, value|
  %div{class: "alert alert-#{key} bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative", role: "alert"}
    %button.close{"data-dismiss": "alert", 'aria-label': "close"}
      %span{'aria-hidden': 'true'} ×
    %ul
      %li
        = value
